import 'package:flutter/material.dart';

import '../widgets/video_content2.dart';

class VideoScreen2 extends StatelessWidget {
  const VideoScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: VideoContent2(),
    );
  }
}
