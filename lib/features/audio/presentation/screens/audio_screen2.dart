import 'package:flutter/material.dart';

import '../widgets/audio_content2.dart';

class AudioScreen2 extends StatelessWidget {
  const AudioScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: AudioContent2(),
    );
  }
}
